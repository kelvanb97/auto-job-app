// Job card in the left list panel
export const CARD_SELECTOR = [
	'div[jsname="y1Aese"][role="button"]',
	'main div[role="button"][data-ved]',
].join(", ")

// Detail panel container that appears when a card is clicked
export const DETAIL_PANEL_SELECTOR = [
	'div[jsname="ujKaBc"]',
	'div[jsname="lHGsle"]',
].join(", ")

// Apply links within the detail panel
export const APPLY_LINK_SELECTOR = [
	'div[jsname="s2gQvd"] a',
	'div[jsname="m3CPue"] a',
].join(", ")
